<menu xmlns:android="http://schemas.android.com/apk/res/android">
    <group android:checkableBehavior="single">
        <item
            android:id="@+id/law"
            android:checked="true"
            android:icon="@mipmap/ic_launcher"
            android:title="Закон"/>
        <item
            android:id="@+id/manual"
            android:icon="@mipmap/ic_launcher"
            android:title="Посібник"/>
    </group>

</menu>